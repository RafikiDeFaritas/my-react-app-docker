services:
  # ============= Backend Express (Web Service) =============
  - type: web
    name: myapp-api
    env: node
    rootDir: server
    plan: free
    region: frankfurt
    buildCommand: npm ci --omit=dev
    startCommand: node index.js
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      # Ajoute DATABASE_URL manuellement après déploiement (Neon)
      - key: CORS_ORIGIN
        value: "*"

  # ============= Frontend React (Static Site) =============
  - type: static_site
    name: myapp-web
    rootDir: .
    buildCommand: |
      npm ci
      npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: "/*"
        destination: /index.html
    envVars:
      - key: VITE_API_BASE
        fromService:
          type: web
          name: myapp-api
          property: url
